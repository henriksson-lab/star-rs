[package]
name = "star-rs"
version = "0.1.0"
edition = "2024"

[features]
default = []
tracehash = ["dep:tracehash-rs"]

[dependencies]
clap = { version = "4.5", features = ["derive"] }
crc32fast = "1.5"
flate2 = "1.0"
libc = "0.2"
tracehash-rs = { version = "0.2.0", optional = true }
